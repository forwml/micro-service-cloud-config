spring:
    profiles:
        active:
            - dev

---
spring:
    profiles: dev
    application:
        name: micro-service-cloud-config-wml-dev

---
spring:
    profiles: test
    application:
        name: micro-service-cloud-config-wml-test

#保存utf-8格式,系统